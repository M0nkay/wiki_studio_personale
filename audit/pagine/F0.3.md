# Audit di rigore — F0.3 Logaritmi

## A. Verdetto
Definizioni/enunciati esaminati: 5 (def. di `\log_b c`, regola d'oro, regola gemella `log(a^n)`, valori speciali `log_b 1`, abbreviazione `\ln`). Severità dopo verifica avversariale: **1 BLOCCANTE, 4 GRAVE, 4 MINORE**. Tutta l'aritmetica (esempi ed esercizi, `python3` cifra per cifra) è corretta; il difetto è sistemico e non numerico: **la pagina non enuncia MAI le condizioni di esistenza** (`b>0`, `b≠1`, `c>0`), la definizione §2 usa l'articolo determinativo «l'esponente» (presuppone esistenza+unicità), §3 afferma il **falso** «la base può essere qualsiasi numero», la regola d'oro §4 e la gemella si rompono sui casi estremi, e **nessun box «Onestà»** dichiara la rinuncia (regola 5). Glossario: blocco Gradino 0 assente e «prime occ.» errate. **NON promuovibile a VERIFICATA.**

## B. Tabella di triage
| ID | § | Oggetto | Codici | Severità |
|----|----|---------|--------|----------|
| F0.3-02 | §3 | «la base può essere qualsiasi numero» — falso | E, I | BLOCCANTE |
| F0.3-01 | §2 | Def. di `\log_b c` — dominio e buona posizione | D1, D3, D6, I, P | GRAVE |
| F0.3-03 | §4 | `log(a·b)=log a+log b` — positività mancante | I, D6 | GRAVE |
| F0.3-04 | §4 deep | `log(a^n)=n·log a` — rotta per base negativa | E, I, D6 | GRAVE |
| F0.3-08 | tab. + Glossario | Gradino 0 assente; «prime occ.» errate | G, O | GRAVE |
| F0.3-05 | §2 box | «`log_b 1 = 0` sempre» — quantificatore non ristretto | E, I | MINORE |
| F0.3-06 | §1–§4 | Titoli liberi dei box (tassonomia) | S, T | MINORE |
| F0.3-07 | §4 | «Intuizione.» inline — lessico vietato T2 | T, T2 | MINORE |
| F0.3-09 | §3 | `e` introdotto ma mai definito; rinuncia non dichiarata | I, D1 | MINORE |

## C. Schede

### F0.3-02 — §3 «la base può essere qualsiasi numero» [BLOCCANTE]
**Testo attuale** (riga 177): «La base può essere qualsiasi numero, ma nella wiki compaiono solo due, con abbreviazioni proprie:»
**Difetti** — E (affermazione falsa), I (condizione `b>0, b≠1` mai scritta). Regola C: a livello F0.x la falsa generalizzazione è un difetto E anche sul pavimento; severità «falsa» → BLOCCANTE.
**Perché** — Ricalcolo `python3`: `b=1` → `set(1**x for x in range(20)) == {1}`: nessun esponente dà 8 (`log_1 8` inesistente), e `log_1 1` non è unico; `b=0` → `math.log(8,0)` solleva `ValueError`; `b=-2` → `math.log(-2,10)` `ValueError`, `(-2)^x` non è reale per `x` non intero. La base deve essere `>0` e `≠1`.
**Riscrittura** — «La base dev'essere un numero `\(>0\)` e diverso da `\(1\)` (per `\(b=1\)` la potenza `\(1^x\)` vale sempre `\(1\)` e non si può invertire; per `\(b\le0\)` non è una potenza reale). Tra le basi ammesse, nella wiki ne compaiono solo due, con abbreviazioni proprie:»
**Impatto a valle** — 1.1 (`\ln`), F3 (`\log_2`), F4 (`-\ln L`): ereditano l'idea tacita che la base sia libera.

### F0.3-01 — §2 Definizione di `\log_b c` [GRAVE]
**Testo attuale** (riga 165): «`\(\log_b c\)` è l'esponente da dare a `\(b\)` per ottenere `\(c\)`.»
**Difetti** — D1/D3/P (l'articolo «l'esponente» presuppone esistenza+unicità, garantite solo per `b>0, b≠1, c>0`), D6/I (dominio mai scritto, nessun box «Onestà»).
**Perché** — Prova del coprire: coperta la frase, resta un'idea genuina (inverso della potenza) ma senza dominio è mal posta. Controesempi `python3`: `math.log(0,2)` e `math.log(-8,2)` sollevano `ValueError` (`2^x>0 ∀x`); per `b=1` l'esponente non è unico. La definizione, presa alla lettera, è mal posta fuori dal dominio implicito.
**Riscrittura** — «Definizione. Siano `\(b>0\)` con `\(b\neq1\)` e `\(c>0\)`. Il logaritmo `\(\log_b c\)` è l'UNICO esponente `\(x\)` tale che `\(b^x=c\)` (esiste ed è unico perché, con `\(b>0,\ b\neq1\)`, la potenza `\(b^x\)` assume ogni valore positivo una sola volta). Fuori da queste condizioni — base `\(\le0\)` o `\(=1\)`, argomento `\(\le0\)` — il logaritmo non è definito.»
**Impatto a valle** — 1.1 §5.2 (`\ln` di probabilità: `p=0` → `\ln 0=-\infty`, escluso solo da `c>0`); F3 §2 (la convenzione `0·\log_2 0:=0` tampona a valle il `\log 0` che questa pagina-sorgente non dichiara indefinito); F4 §2.3 (log-loss `-\ln L`).

### F0.3-03 — §4 Regola d'oro `log(a·b)=log a+log b` [GRAVE]
**Testo attuale** (riga 190): `\[ \log(a \cdot b) = \log a + \log b \]`
**Difetti** — I/D6 (ipotesi `a>0, b>0` e stessa base mai scritte prima della formula; D6 elenca esplicitamente «argomenti positivi sotto log»).
**Perché** — Prova del nove `python3`: `a=-2, b=-3` → `a·b=6>0`, sinistra `math.log(6,10)=0.778…` DEFINITA, destra `log10(-2)+log10(-3)` → `ValueError`: l'identità presa alla lettera è rotta. `a=0` → sinistra `math.log(0,10)` `ValueError`.
**Riscrittura** — «Per `\(a>0\)` e `\(b>0\)`, e con la stessa base su tutti i logaritmi: `\[\log(a\cdot b)=\log a+\log b.\]` (La positività serve: entrambi i logaritmi a destra devono esistere.)»
**Impatto a valle** — 1.1 §5 (somma di `\ln` di probabilità: valida solo perché le prob. usate sono `>0`, ipotesi da enunciare a monte).

### F0.3-04 — §4 box «Più a fondo» — regola gemella `log(a^n)=n·log a` [GRAVE]
**Testo attuale** (riga 201): «E `\(\log(a^n) = n \cdot \log a\)` (l'esponente «scende» a moltiplicare).»
**Difetti** — I/D6 (manca `a>0`, e `b>0` per la divisione) più E (errore vero per base negativa).
**Perché** — `python3`: `a=-2, n=2` → `a^n=4`, sinistra `math.log(4,10)=0.602…` definita, destra `2·log10(-2)` → `ValueError`; la forma corretta è `\(\log(a^n)=n\log|a|\)` quando `a^n>0` (verificato: `2*log10(2)=0.60206=log10(4)`). Scritta com'è, la destra è indefinita dove la sinistra esiste.
**Riscrittura** — «Per `\(a>0,\ b>0\)`: `\(\log\dfrac{a}{b}=\log a-\log b\)` e `\(\log(a^n)=n\log a\)`. (Per `\(a<0\)` con `\(a^n>0\)` vale `\(\log(a^n)=n\log|a|\)`: `\(\log a\)` da solo non esiste.)»
**Impatto a valle** — 1.1 §5.2 (logit `\ln\frac{p}{1-p}`), F3 (PMI `\log_2` di un rapporto): il rapporto va tenuto `>0`.

### F0.3-08 — Tabella «Simboli introdotti» + Glossario globale [GRAVE]
**Testo attuale** (righe 234–237, tabella locale): prima occ. `\log_2`=F0.3 §3, `\ln,e`=F0.3 §3 — ma il Glossario (verificato) dà `e,\ln`→1.1 §5.2 (riga 191), `\log_2`→F3 §1 (riga 270).
**Difetti** — G (blocco Gradino 0 assente dal Glossario), O (prime occ. incongruenti con l'ordine di studio).
**Perché** — (1) Sezione C dell'output meccanico: i simboli propri di F0.3 (`\log_b c`, regola d'oro `\log(ab)=\log a+\log b`) non sono censiti in nessuna riga del Glossario. (2) Ordine di studio: F0.3 (pos. 3) precede 1.1 (pos. 9) e F3 → le prime occ. `e,\ln`→1.1 e `\log_2`→F3 sono sbagliate; corretta = F0.3 §3 (sezione D dell'output). (3) `\Longleftrightarrow` è usato in F0.3 §1 (riga 156) ma **manca** dalla tabella locale; il Glossario (riga 301) lo attribuisce a 1.4 §4 → prima occ. sbagliata (dovrebbe essere F0.3 §1), e la glossa locale «è la stessa cosa di» (riga 157) diverge da quella del Glossario «se e solo se».
**Riscrittura** — Aggiungere al Glossario il blocco «Gradino 0» con `\log_b c`, `\log_2`, `\ln`, `e`, `\Longleftrightarrow`, regola d'oro; correggere le prime occ.: `e/\ln`→F0.3 §3, `\log_2`→F0.3 §3, `\Longleftrightarrow`→F0.3 §1. Aggiungere `\Longleftrightarrow` alla tabella locale di F0.3 e allineare la glossa a «se e solo se».
**Impatto a valle** — Glossario globale (fonte di verità unica): ogni pagina che rimanda a `e/\ln/\log_2` eredita l'attribuzione errata della prima occorrenza.

### F0.3-05 — §2 box «Due valori speciali» — «`log_b 1 = 0` sempre» [MINORE]
**Testo attuale** (riga 171): «`\(\log_b 1 = 0\)` sempre: perché `\(b^0 = 1\)` (F0.2)…»
**Difetti** — E/I («sempre» è un quantificatore universale non ristretto).
**Perché** — Falso per `b=1` (`1^x=1 ∀x`: l'esponente non è unico e `\log_1 b=1` coesisterebbe) e privo di senso per `b\le0`; anche «`b^0=1`» richiede `b\neq0`. Vale solo entro `b>0, b\neq1`.
**Riscrittura** — «Per ogni base ammessa (`\(b>0,\ b\neq1\)`): `\(\log_b 1=0\)`, perché `\(b^0=1\)`; e `\(\log_b b=1\)`.»
**Impatto a valle** — Locale; rinforza F0.3-01/02.

### F0.3-06 — Titoli liberi dei box (tassonomia, regola E) [MINORE]
**Testo attuale** — «Una frase che risolve tutto» (intu §1, riga 158); «Due valori speciali» (ex §2, riga 170); «Attenzione alla scrittura» (warn §3, riga 182); «Più a fondo (opzionale) — la regola gemella» (deep §4, riga 200); «Esempio numerico — passo per passo / — la regola in azione» (ex §2/§4, righe 166/192).
**Difetti** — S/T (regola E: i titoli liberi sono un difetto; la classe del box è canonica, il testo del tag va normalizzato). FASE A ne aveva colti solo due; aggiungo warn §3 e deep §4 e i suffissi narrativi sui box Esempio.
**Perché** — Box canonici: *Intuizione, Esempio numerico, Trappola/Attenzione, Approfondimento (opzionale), N.B., Onestà*. I tag narrativi non sono canonici.
**Riscrittura** — «Una frase che risolve tutto»→«Intuizione»; «Due valori speciali»→«Esempio numerico» (o «N.B.»); «Attenzione alla scrittura»→«Trappola»; «Più a fondo (opzionale) — …»→«Approfondimento (opzionale)»; togliere i suffissi «— passo per passo / — la regola in azione».
**Impatto a valle** — Nessuno (stile di casa).

### F0.3-07 — §4 paragrafo «Intuizione.» inline — lessico vietato T2 [MINORE]
**Testo attuale** (riga 191): «**Intuizione.** Il logaritmo smonta le moltiplicazioni in addizioni. E perché è utile? … È il trucco che nella 1.1 (§5.2) evita la «degenerazione numerica».»
**Difetti** — T/T2 (regola F: «Intuizione.» in grassetto inline non è un box, quindi la domanda retorica «E perché è utile?» e «il trucco» sono lessico vietato T2 nel corpo).
**Perché** — Regola F + T2: domande retoriche e registro colloquiale fuori dai box sono un difetto sistematico.
**Riscrittura** — Spostare in un box «Intuizione» reale: «La somma di logaritmi resta in una scala gestibile, dove il prodotto di molte probabilità collasserebbe verso zero (1.1 §5.2).» — senza domanda retorica. (Anche riga 189 «Questa è **la** ragione…» è enfatica ma non nel lessico vietato: lasciata.)
**Impatto a valle** — Nessuno (registro).

### F0.3-09 — §3 `e` introdotto ma mai definito; rinuncia non dichiarata [MINORE]
**Testo attuale** (riga 180): «Qui `\(e \approx 2{,}718\)` è un numero speciale della matematica (come `\(\pi\)`).»
**Difetti** — I/D1 (di `e` si dà solo un nome e un'approssimazione, mai il tipo/definizione: quale numero reale? limite/serie? — cfr. inventario, «caratterizzazione spacciata»); regola 5 (rinuncia non dichiarata in un box «Onestà»).
**Perché** — `e` non è nel pavimento esteso dichiarato dal charter (che elenca `∑ ∏ min max ⌊·⌋` ecc., non `e`); usarlo solo come etichetta senza definirlo né dichiarare la rinuncia è un difetto MINORE per regola 5. La riga finisce nella colonna «definizione» del Glossario locale (riga 236) pur non essendo una definizione.
**Riscrittura** — Aggiungere un box «Onestà»: «`\(e\)` (numero di Nepero, `\(\approx 2{,}718\)`) è qui trattato come costante nota, come `\(\pi\)`; la sua definizione (limite/serie) è fuori dal livello F0.x.» — oppure censire `e` nel pavimento dichiarato.
**Impatto a valle** — Locale; vedi anche domanda G2.

## D. Prova del nove

| dove | valore sulla pagina | valore ricalcolato (`python3`) | esito |
|------|--------------------|-------------------------------|-------|
| §1 `log_2 8` | 3 | `math.log(8,2)`=3.0 | OK |
| §2 `log_10 1000` | 3 | 2.9999999999999996 (float 4e-16) | OK |
| §2 `log_2 4` | 2 | 2.0 | OK |
| §2 `log_10 100` | 2 | 2.0 | OK |
| §2 `log_b 1` (b=2,10,5) | 0 | [0.0,0.0,0.0] | OK |
| §2 `log_b b` (b=2,10,5) | 1 | [1.0,1.0,1.0] | OK |
| §3 `e ≈ 2,718` | 2,718 | `round(math.e,3)`=2.718 | OK |
| §4 `100·1000=10^5` | 100000 | 100000, True | OK |
| §4 `log_10 100000` | 5 | 5.0 | OK |
| §4 `log_10 100+log_10 1000` | 5 | 5.0 | OK |
| Es.1 `log_2 16` | 4 | 4.0 | OK |
| Es.2 `log_10 10000` | 4 | 4.0 | OK |
| Es.3 `log_5 1` | 0 | 0.0 | OK |
| Es.5 `log(7·3)=log7+log3` | identità | `log10(21)-(log10(7)+log10(3))`=0.0 | OK |

**Casi estremi provati (rompono le formule prese alla lettera):**
- Base = 1: `set(1**x for x in range(20))=={1}` → `log_1 8` inesistente, `log_1 1` non unico → **smentisce §3** e «`log_b 1=0` sempre».
- Base = 0: `math.log(8,0)` → `ValueError` → `log_0 8` inesistente.
- Base negativa: `math.log(-2,10)` → `ValueError`; `(-2)^x` non reale per `x` non intero.
- Argomento 0: `math.log(0,2)` → `ValueError` (limite `-∞`); mai dichiarato indefinito (richiesto dal charter, «log di 0»).
- Argomento negativo: `math.log(-8,2)` → `ValueError` (`2^x>0 ∀x`).
- Regola d'oro con fattori negativi a prodotto positivo (`a=-2,b=-3`): sinistra `log10(6)=0.7782` definita, destra `ValueError` → **identità rotta**.
- Regola d'oro con fattore 0 (`a=0`): sinistra `log10(0)` `ValueError`.
- Gemella `log(a^n)`, `a=-2,n=2`: sinistra `log10(4)=0.6021` definita, destra `2·log10(-2)` `ValueError`; corretto `n·log|a|=0.6021` → **rotta per `a<0`**.

## E. Conformità al Glossario
- **Blocco «Gradino 0» ASSENTE** dal Glossario (sezione B output meccanico, regola G): i simboli propri di F0.3 — `\log_b c` e la regola d'oro `\log(ab)=\log a+\log b` — non compaiono in nessuna riga (sezione C).
- **Prime occ. errate (O)**: `e,\ln` → Glossario riga 191 dà 1.1 §5.2, corretta F0.3 §3; `\log_2` → Glossario riga 270 dà F3 §1, corretta F0.3 §3 (F0.3 pos. 3 precede 1.1 e F3).
- **`\Longleftrightarrow`**: usato in F0.3 §1 (riga 156) ma **assente dalla tabella locale**; Glossario riga 301 lo attribuisce a 1.4 §4 → prima occ. sbagliata (dovrebbe essere F0.3 §1); glossa locale «è la stessa cosa di» diverge da quella del Glossario «se e solo se».
- Nessuna collisione (sezione E output) coinvolge simboli introdotti da F0.3 (`\log_b c, \log_2, \ln, e` sono nuovi): nessun box Trappola dovuto su questa pagina.
- La definizione locale di `\ln` (§3: `\ln=\log_e`, `e≈2,718`) è coerente col Glossario; ma il Glossario (riga 191) aggiunge «negativo su `(0,1)`», assente da F0.3 — il dominio `arg>0` resta implicito in entrambi.

## F. Senza rilievi
`§2-esempi` (log_10 1000; log_2 4; log_2 8; log_10 100), `§4-esempio` (100·1000=10^5; log_10 100000=5; 2+3=5), `Es.1–5`, `§3-def-ln` (`\ln := \log_e`), `§1-cerniera` (`2^3=8 ⟺ log_2 8=3`).

## G. Domande all'autore
1. Il livello F0.x tratta le condizioni `b>0, b≠1, c>0` come «pavimento informale» (date per note) o vanno enunciate? Allo stato non c'è né enunciato né box «Onestà» che dichiari la rinuncia — la regola 5 lo richiederebbe (radice comune di 01–05).
2. `e` è una costante del pavimento (introdotta solo per approssimazione `≈2,718`, «come `\pi`»)? Se sì va aggiunto al pavimento dichiarato; altrimenti manca una definizione di `e` o un box «Onestà» (F0.3-09).
3. La tabella «Simboli introdotti» colloca la regola d'oro nella colonna «definizione»: scelta voluta trattare un'identità/proprietà come voce-simbolo, o va spostata come proprietà?
